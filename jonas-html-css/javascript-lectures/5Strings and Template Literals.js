const country = "Iran";
const language = "Persion";
const population = 100;
const continent = "Asia";

const discription = `${country} is in ${continent}, and its ${population} million people speak ${language}.`;
console.log (discription);